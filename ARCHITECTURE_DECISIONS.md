# 架構決策清單

請審閱並確認以下設計決策。如果不同意，我們可以調整。

---

## 🎯 核心設計決策

### 1️⃣ **四層架構是否合理？**

```
數據來源層 → 處理引擎層 → 存儲層 → 用戶界面層
```

- ✅ 清晰的數據流
- ✅ 易於擴展（未來可加新源或新UI）
- ✅ 各層相互獨立（可並行開發）

**你同意嗎？** → [ ] Yes [ ] 需要調整

---

### 2️⃣ **數據源優先級**

**現在（Phase 1-2）：**
1. 用戶上傳 PDF（已完成 ✅）
2. Finnhub API（國際分析師評級）
3. NewsAPI（投顧新聞）
4. Web Scraper（SeekingAlpha、台灣網站）

**未來（可選）：**
5. Bloomberg / FactSet（企業級，高成本）

**你的偏好是？**
- [ ] A) 先做 API（簡單、穩定、無 IP 風險）
- [ ] B) 先做爬蟲（覆蓋更多網站，難度高）
- [ ] C) 同時做兩者（並行，工作量大）
- [ ] D) 只做 API（忽略爬蟲）

---

### 3️⃣ **數據存儲方案**

**選項 A：本地 JSON 文件**（推薦）
```
data/
├── reports.json       （所有報告元數據）
├── ratings.json       （分析師評級聚合）
├── stocks_mapping.json（股票代碼映射）
└── cache/             （API 快取）
```
- 優點：簡單、無 DB 維護、易備份
- 缺點：查詢速度有限（>10k 報告時）
- 成本：$0
- 擴展性：可升級為 SQLite（未來）

**選項 B：本地 SQLite 數據庫**
- 優點：查詢速度快、功能豐富
- 缺點：需 DB 維護、備份麻煩
- 成本：$0
- 擴展性：易升級為 PostgreSQL

**選項 C：云數據庫（Firebase / DynamoDB）**
- 優點：自動備份、實時同步
- 缺點：$$$（月費 10-50 美元）
- 擴展性：無限
- 適合：多設備同步

**你的選擇？**
- [ ] A) JSON 文件（輕量級）
- [ ] B) SQLite（平衡）
- [ ] C) 云數據庫（企業級）

---

### 4️⃣ **用戶界面方案**

**選項 A：Streamlit（推薦）**
```
Streamlit 多頁應用
├── 00_Dashboard.py    （統計首頁）
├── 01_Search.py       （搜索報告）
├── 02_Stock.py        （單股分析）
└── 03_Settings.py     （任務管理）
```
- 優點：快速開發（1-2 天）、Python 原生
- 缺點：功能受限（不適合複雜交互）
- 成本：$0（本地）或 Streamlit Cloud $5/月
- 界面質量：★★★☆☆

**選項 B：React + FastAPI**
```
React 前端（Next.js）
│
FastAPI 後端（API）
│
數據層
```
- 優點：高度自定義、企業級質量
- 缺點：開發時間長（2-3 週）
- 成本：$0（本地）或 Vercel + Railway $10-20/月
- 界面質量：★★★★★

**選項 C：簡單 HTML + Flask**
```
Flask 應用
├── HTML 模板
├── CSS 樣式
└── JavaScript 交互
```
- 優點：輕量、無框架依賴
- 缺點：功能簡陋
- 成本：$0
- 界面質量：★★☆☆☆

**你的選擇？**
- [ ] A) Streamlit（推薦，最快上手）
- [ ] B) React + FastAPI（質量最佳，時間長）
- [ ] C) Flask（最簡單）

---

### 5️⃣ **更新頻率**

**選項 A：定時批量（推薦）**
- 每天 3 次自動同步（08:00, 12:00, 18:00）
- 優點：API 成本低、對系統負擔輕
- 缺點：最新數據有延遲（最多 8 小時）

**選項 B：實時更新**
- 有新報告立即更新
- 優點：數據最新
- 缺點：API 成本高、系統負擔重
- 成本：+100% API 費用

**選項 C：混合**
- API：實時（Finnhub 有新評級立即同步）
- 爬蟲：定時（每天 2 次）
- 成本：中等

**你的選擇？**
- [ ] A) 定時批量（推薦）
- [ ] B) 實時更新
- [ ] C) 混合方案

---

### 6️⃣ **Web Scraper 方案**

**選項 A：使用 Selenium（完整）**
- 模擬瀏覽器完全加載頁面
- 優點：支持 JavaScript 動態網站
- 缺點：慢（每頁 3-5 秒）、IP 易被封
- 風險：⚠️ 某些網站會偵測爬蟲並封 IP
- 成本：$0（或 $3-5/月 用代理 IP）

**選項 B：使用 BeautifulSoup（輕量）**
- 直接解析 HTML
- 優點：快速（每頁 <1 秒）、資源輕
- 缺點：不支持 JavaScript
- 風險：✅ 較安全（看起來像正常流量）
- 成本：$0

**選項 C：使用 Puppeteer（JavaScript 版本）**
- 類似 Selenium，但更快
- 優點：速度介於兩者之間
- 缺點：需 Node.js，Python 集成複雜
- 成本：$0 或 $3-5/月（代理）

**選項 D：不爬蟲，只用 API**
- 只用 Finnhub + NewsAPI
- 優點：✅ 完全合法、零風險
- 缺點：❌ 無法爬 SeekingAlpha
- 覆蓋率：~70%

**你的選擇？**
- [ ] A) Selenium（完整，有風險）
- [ ] B) BeautifulSoup（安全，有限制）
- [ ] C) Puppeteer（平衡）
- [ ] D) 不爬蟲，只用 API（保守）

---

### 7️⃣ **成本預算**

**月費預估：**

| 方案 | 組件 | 月費 |
|------|------|------|
| **最低成本** | Finnhub Free + NewsAPI Free + Streamlit 本地 | $0 |
| **推薦方案** | Finnhub Pro + NewsAPI + Streamlit Cloud | $14 |
| **企業級** | Finnhub Pro + 代理 IP + Vercel Deploy | $25-50 |

**你的預算？**
- [ ] $0 - 完全免費（有限制）
- [ ] $10-20 - 小投資（推薦）
- [ ] $50+ - 無限制

---

### 8️⃣ **時間承諾**

**選項 A：完整實施（Phase 1-4）**
- 時間：6-10 天（全職）或 2-3 週（兼職 2h/day）
- 交付物：完整的投顧報告系統

**選項 B：MVP 快速版（Phase 1 + 3 + UI 簡版）**
- 時間：2-3 天（全職）
- 交付物：API 聚合 + 基礎搜索

**選項 C：持續迭代**
- 時間：每週 1-2 小時
- 交付物：逐步功能增強

**你的時間安排？**
- [ ] A) 立即全力實施（接下來 10 天）
- [ ] B) 快速 MVP（接下來 3 天）
- [ ] C) 慢速迭代（每週更新）

---

## 📋 確認清單

請確認以下所有項：

- [ ] 四層架構理解並同意
- [ ] 數據源優先級明確（選擇了 A/B/C/D）
- [ ] 存儲方案確定（選擇了 A/B/C）
- [ ] UI 框架選擇完成（選擇了 A/B/C）
- [ ] 更新頻率決定（選擇了 A/B/C）
- [ ] Web Scraper 方案決定（選擇了 A/B/C/D）
- [ ] 成本預算認可（選擇了金額）
- [ ] 時間承諾確認（選擇了速度）

---

## 🚀 接下來怎麼做？

**確認後，我會：**

1. **根據你的選擇調整架構**（如有特殊要求）
2. **開始 Phase 1：API 集成**
   - 編寫 Finnhub 集成模塊
   - 編寫 NewsAPI 集成模塊
   - 設置定時任務框架
   - 生成 Markdown 模板

3. **並行開始 Phase 2：Web Scraper**（如果你選了爬蟲）

4. **在 Streamlit 儀表盤中展示數據**

5. **每天提交進度報告**

---

## 💬 任何疑問嗎？

請告訴我：
- 哪個決策不清楚？
- 有其他考慮嗎？
- 想調整任何選擇嗎？

**一旦你確認所有決策，我們就可以開始編碼了！** 🚀
