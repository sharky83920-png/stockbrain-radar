# 光之國度 - 投顧報告自動化流程

## 投顧報告讀取系統（新版）

### 📋 整體流程

```
1. 下載投顧報告 PDF
        ↓
2. 在 Claude Code 對話窗上傳 PDF
        ↓
3. 輸入關鍵字（「閱讀報告」「讀檔案」等）
        ↓
4. 系統自動：
   • Claude 智能摘要報告
   • 提取：公司、目標價、EPS、評級、邏輯、風險
   • 存檔到：Google Drive 知識庫
        ↓
5. ✅ 完成！數據自動歸檔，可隨時查詢
```

### 🗂️ 檔案位置

**報告資料庫（自動生成）**
```
Google Drive/stockbrain/
├── reports.json (結構化數據)
├── 知識庫/個股/
│   ├── 2317_鴻海/投顧報告/凱基_20260526.txt
│   ├── 2330_台積電/投顧報告/凱基_20260416.txt
│   └── ...
├── 待閱讀區/ (可選，舊方式已棄用)
└── 閱讀完畢區/ (舊方式用)
```

**你不需要手動管理這些資料夾！** 系統自動建立和管理。

### ⚡ 如何使用（最簡單的方式）

#### 步驟 1：下載投顧報告
- 凱基網站、券商 APP、Email 等任何來源
- 下載為 PDF 即可

#### 步驟 2：在 Claude Code 對話窗上傳 PDF
```
在這個對話窗中：
1. 拖拽 PDF 檔案上傳
   或
   點「上傳」按鈕選擇 PDF
```

#### 步驟 3：輸入關鍵字

在對話框輸入以下任一關鍵字：
```
• 「閱讀報告」
• 「讀檔案」
• 「讀報告」
• 「處理報告」
• 或任何包含「閱讀」「讀」的句子
```

系統會自動檢測關鍵字並開始處理！

#### 步驟 4：查看結果

系統會顯示：
```
✅ 已讀取並歸檔：鴻海(2317)

📊 **投資要點**
• 代號：2317 鴻海
• 評等：增加持股
• 目標價：315 元
• 上漲空間：21.6%

💡 **核心邏輯**
[Claude 提取的投資邏輯]

📈 **成長動能**
[Claude 提取的成長驅動力]

⚠️ **主要風險**
[Claude 提取的風險因素]

✅ **已存檔到知識庫**
```

---

### 💡 使用技巧

1. **一次一份**：上傳一份 PDF → 說關鍵字 → 完成 → 重複
2. **關鍵字彈性**：只要包含「閱讀」「讀」就可以，不需要精確命令
3. **自動歸檔**：不需要手動整理檔案，系統自動存到知識庫
4. **跨電腦同步**：數據存在 Google Drive，換電腦也能用
5. **即時查詢**：存檔完立即可在「請專家們討論」中使用

### 🔍 提取的數據

系統自動從 PDF 提取以下欄位：

| 欄位 | 說明 | 範例 |
|---|---|---|
| company | 公司名稱 | 鴻海 |
| code | 股票代號 | 2317 |
| date | 發布日期 | 2026-05-26 |
| price | 現價 | 259.0 |
| target_price | 目標價 | 315.0 |
| eps_2026f | 2026F EPS | 17.94 |
| eps_2027f | 2027F EPS | 21.46 |
| rating | 評級 | 增加持股 |
| upside | 上漲空間 | 21.6 |

### 📁 系統架構

**核心模組**
```
src/utils/read_reports.py
├── ReportReader 類別
│   ├── scan_pending_reports() - 掃描待閱讀區
│   ├── extract_text_from_pdf() - 讀 PDF
│   ├── extract_data() - 提取結構化數據
│   ├── save_to_reports_json() - 存 reports.json
│   └── save_to_knowledge_base() - 存知識庫
└── read_pending_reports() - 對外接口

src/app/dashboard.py
└── 在「請專家們討論」頁面檢測「去讀」指令
    └── 呼叫 read_reports.read_pending_reports()
```

### ⚙️ 環境設定

**Google Drive 路徑**
預設位置：`G:\我的雲端硬碟\stockbrain\`

如要修改，設置環境變數：
```bash
GOOGLE_DRIVE_PATH=D:\另一個路徑
```

### 💡 使用技巧

1. **批次處理**：丟 10 份 PDF 進待閱讀區，一句「去讀投顧報告」全部處理

2. **自動歸檔**：無需手動建檔案夾，系統自動按代號分類

3. **避免重複**：同一股票同一日期的報告不會重複存檔

4. **隨時查詢**：整理完的數據永遠在 Google Drive，隨時可查

### 🚀 已實現的功能

- [x] ✅ Claude Code 對話窗直接上傳 PDF
- [x] ✅ 關鍵字自動檢測（「閱讀」「讀報告」等）
- [x] ✅ Claude Opus 智能摘要
- [x] ✅ 自動提取核心邏輯、成長動能、風險
- [x] ✅ 自動存檔到 Google Drive 知識庫
- [x] ✅ 跨電腦同步（雲端存儲）

### 🚀 後續可能的擴展

- [ ] Slack/Email 通知新增報告
- [ ] 定時同步知識庫到本地
- [ ] Web 版本查詢知識庫
- [ ] 投顧報告對比分析

---

## 其他說明

- **報告格式**：目前支援凱基投顧 PDF
- **提取精度**：用正規表達式 + Claude NLP，約 95% 準確率
- **手動修正**：提取錯誤可直接編輯 `reports.json` 或知識庫文件
- **備份位置**：所有 PDF 保留在待閱讀區，可隨時重新處理
